{% extends "@OrkestroWeb/Backend/base.html.twig" %}
{% trans_default_domain "backend" %}

{% block body -%}
    <h1>Locale edit</h1>
    <h2>{{ entity.title }}</h2>

    {{ form(edit_form) }}

    <ul class="record_actions">
    <li>
        <a href="{{ path('orkestro_backend_locale_list') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
