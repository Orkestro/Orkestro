orkestro:
  locale:
    headers:
      locales: Locales
      new: Create Locale
      code: Code
      title: Title
      enabled: Enabled
      actions: Actions
      fallback: Fallback
    buttons:
      add: Add new locale
    info:
      of: of
